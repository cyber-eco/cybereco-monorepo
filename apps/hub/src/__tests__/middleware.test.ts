/**
 * @jest-environment node
 */

// Skip middleware tests for now due to Next.js server components complexity
describe.skip('Hub Middleware', () => {
  it('should be tested with E2E tests', () => {
    expect(true).toBe(true);
  });
});