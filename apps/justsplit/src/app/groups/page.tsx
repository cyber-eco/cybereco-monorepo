'use client';

import React from 'react';
import PageRedirect from '../../components/ui/PageRedirect';

export default function GroupsPage() {
  return <PageRedirect redirectPath="/groups/list" title="groups list" />;
}
